# Dependencies
node_modules/
.pnpm-store/
.venv/
__pycache__/
*.py[cod]

# Build output
dist/
build/
.next/
out/
*.tsbuildinfo

# Caches
.turbo/
.cache/
.eslintcache
.ruff_cache/
.pytest_cache/
.mypy_cache/
coverage/
htmlcov/
.coverage

# Environment
.env
.env.*
!.env.example

# Editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Local data. The Compose stack keeps state in named volumes, but a bind-mounted
# ./data is a common local variation.
/data/

# Per-machine Compose tweaks (extra ports, a GPU device, a different image).
# Compose merges this automatically and it is nobody else's business.
docker-compose.override.yml
docker-compose.override.yaml
