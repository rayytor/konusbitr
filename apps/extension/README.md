# @konusbitr/extension

WXT-based Chrome extension: send the PDF in the current tab to Konusbitr and
chat with it without leaving the page.

Empty placeholder until **Phase 15 — Distribution and Launch**. It is
deliberately not a pnpm workspace package yet so that `pnpm install` on a clean
clone stays fast and CI has nothing to build here.
