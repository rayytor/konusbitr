{
  "clean-text-10p": [
    {
      "height": 792.0,
      "page": 1,
      "rotation": 0,
      "text": "Clean Text Fixture \u2014 Section 1 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 1",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 2,
      "rotation": 0,
      "text": "Clean Text Fixture \u2014 Section 2 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 2",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 3,
      "rotation": 0,
      "text": "Clean Text Fixture \u2014 Section 3 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 3",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 4,
      "rotation": 0,
      "text": "Clean Text Fixture \u2014 Section 4 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 4",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 5,
      "rotation": 0,
      "text": "Clean Text Fixture \u2014 Section 5 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 5",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 6,
      "rotation": 0,
      "text": "Clean Text Fixture \u2014 Section 6 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 6",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 7,
      "rotation": 0,
      "text": "Clean Text Fixture \u2014 Section 7 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 7",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 8,
      "rotation": 0,
      "text": "Clean Text Fixture \u2014 Section 8 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 8",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 9,
      "rotation": 0,
      "text": "Clean Text Fixture \u2014 Section 9 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 9",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 10,
      "rotation": 0,
      "text": "Clean Text Fixture \u2014 Section 10 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 10",
      "width": 612.0
    }
  ],
  "rotated-a4": [
    {
      "height": 841.89,
      "page": 1,
      "rotation": 0,
      "text": "Upright A4 Page This page has no rotation and is 595 by 842 points.",
      "width": 595.28
    },
    {
      "height": 595.28,
      "page": 2,
      "rotation": 90,
      "text": "highlight drawn a line too low reads as a bug rather than as an answer worth checking. agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a the unrotated page, which is the single convention the parser, the database and the viewer all Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of Rotated Landscape Page",
      "width": 841.89
    },
    {
      "height": 841.89,
      "page": 3,
      "rotation": 0,
      "text": "Upright Again Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking.",
      "width": 595.28
    }
  ],
  "tables-financial": [
    {
      "height": 792.0,
      "page": 1,
      "rotation": 0,
      "text": "Annual Financial Summary Revenue Revenue grew across every reported segment. The table below is the fixture the table tests assert against. Segment 2023 2024 Change Subscriptions 4,120 5,860 +42% Services 1,905 2,110 +11% Licensing 742 689 -7% Total 6,767 8,659 +28% Headcount Region Engineering Sales Support Europe 84 31 22 Americas 126 58 40 Asia Pacific 47 19 15",
      "width": 612.0
    }
  ],
  "text-50p": [
    {
      "height": 792.0,
      "page": 1,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 1 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 1",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 2,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 2 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 2",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 3,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 3 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 3",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 4,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 4 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 4",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 5,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 5 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 5",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 6,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 6 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 6",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 7,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 7 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 7",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 8,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 8 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 8",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 9,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 9 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 9",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 10,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 10 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 10",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 11,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 11 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 11",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 12,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 12 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 12",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 13,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 13 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 13",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 14,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 14 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 14",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 15,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 15 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 15",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 16,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 16 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 16",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 17,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 17 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 17",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 18,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 18 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 18",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 19,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 19 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 19",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 20,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 20 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 20",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 21,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 21 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 21",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 22,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 22 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 22",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 23,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 23 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 23",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 24,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 24 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 24",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 25,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 25 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 25",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 26,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 26 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 26",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 27,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 27 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 27",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 28,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 28 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 28",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 29,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 29 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 29",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 30,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 30 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 30",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 31,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 31 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 31",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 32,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 32 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 32",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 33,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 33 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 33",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 34,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 34 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 34",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 35,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 35 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 35",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 36,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 36 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 36",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 37,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 37 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 37",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 38,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 38 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 38",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 39,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 39 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 39",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 40,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 40 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 40",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 41,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 41 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 41",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 42,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 42 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 42",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 43,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 43 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 43",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 44,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 44 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 44",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 45,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 45 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 45",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 46,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 46 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 46",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 47,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 47 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 47",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 48,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 48 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 48",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 49,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 49 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 49",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 50,
      "rotation": 0,
      "text": "Performance Budget Fixture \u2014 Section 50 Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. 50",
      "width": 612.0
    }
  ],
  "two-column-paper": [
    {
      "height": 792.0,
      "page": 1,
      "rotation": 0,
      "text": "Layout-Aware Parsing of Multi-Column Documents 1. Introduction Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Paragraph 1 of the introduction. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Paragraph 2 of the introduction. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Paragraph 3 of the introduction. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Paragraph 4 of the introduction. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Paragraph 5 of the introduction. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Paragraph 6 of the introduction. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Paragraph 7 of the introduction. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Paragraph 8 of the introduction. 2. Method Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Paragraph 1 of the method.",
      "width": 612.0
    },
    {
      "height": 792.0,
      "page": 2,
      "rotation": 0,
      "text": "Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Paragraph 2 of the method. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Paragraph 3 of the method. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Paragraph 4 of the method. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Paragraph 5 of the method. Konusbitr stores every bounding box in PDF user-space points with the origin at the top left of the unrotated page, which is the single convention the parser, the database and the viewer all agree on. A box that leaves the worker in any other frame is wrong everywhere downstream, and a highlight drawn a line too low reads as a bug rather than as an answer worth checking. Paragraph 6 of the method.",
      "width": 612.0
    }
  ]
}
