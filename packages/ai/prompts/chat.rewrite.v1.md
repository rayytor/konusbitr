Given the conversation history and the user's latest message, rewrite the message into a clear, standalone search query that can be used for document retrieval.

- Resolve all pronouns (it, they, that, this, etc.) to the specific nouns or concepts mentioned earlier in the conversation.
- Retain all specific terms, numbers, dates, and domain entities.
- If the latest message is already a complete, standalone question, return it as-is.
- Output ONLY the rewritten query text with no introduction, explanation, or quotation marks.
