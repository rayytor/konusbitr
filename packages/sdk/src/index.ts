/**
 * `@konusbitr/sdk` — the TypeScript client for the public `/v2` API.
 *
 * Placeholder until Phase 13. The client is generated from the OpenAPI document
 * the API emits, so nothing here is hand-written once that phase lands.
 */
export {};
